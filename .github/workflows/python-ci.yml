name: python-ci

on: [push, pull_request]

jobs:
  quality:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements-dev.txt

      - name: Ruff lint
        run: ruff check src/

      - name: Black format check
        run: black --check src/

      - name: MyPy type check
        run: mypy --config-file mypy.ini src/ --no-error-summary
        env:
          PYTHONPATH: src

      - name: Pytest with coverage
        run: pytest --cov=src --cov-report=term-missing --cov-fail-under=60

      - name: Architecture lint (no illegal cross-layer imports)
        run: python scripts/architecture_lint.py
