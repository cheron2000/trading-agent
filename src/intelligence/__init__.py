"""
intelligence — Intelligence Layer (Athena) for the AI Trading OS.

Converts FeatureVectorEvents into DecisionEvents via rule-based
strategies or an LLM agent, with optional decision memory context.
"""
